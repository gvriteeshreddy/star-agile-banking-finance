
pipeline {
    agent any

    tools {
        // Use the configured Maven version (ensure M2_HOME is set correctly in Jenkins global tool config)
        maven "maven"
    }

    stages {
        stage('Checkout Code') {
            steps {
                // Checkout from GitHub using the specified credentials
                git credentialsId: 'newpass', url: 'https://github.com/gvriteeshreddy/star-agile-banking-finance.git'
            }
        }

       
}
}
